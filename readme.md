# 棒読みちゃんへサウンドファイルを再生するタグを登録するスクリプト

## Environment
+ Python 3.x

## Usage
+ BouyomiChan.exeがあるディレクトリ(ReplaceTag.dic)があるディレクトリにこのスクリプトを配置して以下を実行
```
python bou_sound_generator.py target_dir [priority]
```